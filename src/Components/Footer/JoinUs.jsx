const JoinUs = ({ children }) => {
  return (
    <div className="joinUs-container">
      <span className="joinUs-title">Become a part of The UTSMS Today!</span>
      {children}
    </div>
  );
};

export default JoinUs;
